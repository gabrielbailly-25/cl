services:
  consejo:
    build: .
    container_name: consejo
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "8100:3000"
    volumes:
      - ./data:/app/data
